import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gym_movil_01',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
